/**
 * first - last 
 * 
 * 포함된 양 끝을 의미하는 네이밍 
 * 부터 ~ 까지
 * 
 * 규칙, 연속성이 보장되지 않는 상태 
 */

//ex1.
const students = ['포코', '존', '현석'];

function getStudents(first, last) {
    // .. some code 
}

getStudents(0, 2);

//ex2. DOM 에 firstChild, lastChild  